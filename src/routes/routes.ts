const ROOT = "/";
const ABOUT = "/about";
const WORK = "/work";
const SERVICE = "/service";
const TERMS = "/terms&conditions";
const PRIVACY = "/privacy";

const routes = {
  ROOT,
  ABOUT,
  WORK,
  SERVICE,
  TERMS,
  PRIVACY,
};
export default routes;
